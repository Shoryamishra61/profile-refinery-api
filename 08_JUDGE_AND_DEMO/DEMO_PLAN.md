# Two-Minute Demo

**0:00–0:15** prove no-browser dependency tree/source scan.

**0:15–0:35** architecture: URL -> registry -> direct HTTP -> assembly -> normalization -> schema gate.

**0:35–1:05** live deployed call on consented profile. Show mandatory fields, nested history, image, provenance/observed_at.

**1:05–1:25** deterministic optional-operation drift: core remains, `partial=true`, affected section typed.

**1:25–1:45** live benchmark table with sample size + field recall + experience/education recall + live p50/p95 + calls/profile; fixture metrics shown separately.

**1:45–2:00** close: upstream reverse engineering is isolated behind operation registry/parser contracts, so public API remains stable when LinkedIn changes.

If live upstream is unavailable, explicitly say so; run fixture demo and show last controlled-live report separately.
